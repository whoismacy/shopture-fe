apiVersion: v1
kind: Service
metadata:
  name: frontend-shopture-service
spec:
  selector:
    app: frontend-shopture # This must match the labels in your Deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
  type: ClusterIP
